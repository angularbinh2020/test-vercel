export type ContactModel = {
  currentPageId: number | string;
  fullname: string;
  firstName: string;
  lastName: string;
  email: string;
  tel: string;
  messages: string;
};

export type ContactProjectModel = {
  currentPageId: number | string;
  fullname: string;
  email: string;
  tel: string;
  messages: string;
};
