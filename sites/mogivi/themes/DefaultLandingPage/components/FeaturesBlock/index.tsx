import React from "react";
import {
  GalleryFeature,
  GalleryFeatureProps,
} from "sites/mogivi/layout/components/Gallery/GalleryFeature";

const FeaturesBlock = (props: GalleryFeatureProps) => {
  return <GalleryFeature {...props} />;
};

export default FeaturesBlock;
