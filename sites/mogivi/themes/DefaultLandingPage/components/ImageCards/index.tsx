import React from "react";
import {
  ImageCards,
  ImageCardsProps,
} from "sites/mogivi/layout/components/ImageCardsBlock";

const ImageCardsBlock = (props: ImageCardsProps) => {
  return <ImageCards {...props} />;
};
export default ImageCardsBlock;
