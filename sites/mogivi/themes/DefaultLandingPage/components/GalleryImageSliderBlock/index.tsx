import React from "react";
import {
  GalleryImageSlider,
  GalleryImageSliderProps,
} from "sites/mogivi/layout/components/Gallery/GalleryImageSlider";

const GalleryImageSliderBlock = (props: GalleryImageSliderProps) => {
  return <GalleryImageSlider {...props} />;
};

export default GalleryImageSliderBlock;
