import React from "react";
import {
  CheckList,
  CheckListProps,
} from "sites/mogivi/layout/components/CheckList";

const CheckListBlock = (props: CheckListProps) => {
  return <CheckList {...props} />;
};

export default CheckListBlock;
